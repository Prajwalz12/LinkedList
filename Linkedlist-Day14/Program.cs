using System;

namespace Linkedlist_Day14
{
    class Program
    {
        static void Main(string[] args)
        {
            LinkedList list = new LinkedList();
            list.Add(70);
            list.Display();
            list.Add(30);
            list.Display();
            list.Add(56);
            list.Display();

        }
    }
}
